<% if @article.errors.any? %> 
  <h1>The following errors have prevented the article from being saved</h1>
  <% @article.errors.full_messages.each do |msg| %>
    <p><%= msg %></p>
  <% end %>
<% end  %>

<%= form_with(model: @article, local: true) do |f| %>
  <p>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :description %>
    <%= f.text_area :description %>
  </p>
  <%= f.submit %>
<% end %>

